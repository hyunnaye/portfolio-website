import React, { useState } from 'react';
import Navbar from '../Navbar/Navbar';

const Projects = () => {
    return(
        <div>
            <Navbar />
            <h1>Projects</h1>
        </div>
    );
};

export default Projects;