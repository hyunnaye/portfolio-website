import React, { useState } from 'react';
import Navbar from '../Navbar/Navbar';

const Resume = () => {
    return(
        <div>
            <Navbar />
            <h1>Resume</h1>
        </div>
    );
};

export default Resume;